{
  "name": "comptaflow-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute comptaflow-db --remote --file=./migrations/0001_init.sql",
    "db:init:local": "wrangler d1 execute comptaflow-db --local --file=./migrations/0001_init.sql",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
