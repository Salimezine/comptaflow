services:
  - type: web
    name: comptaflow
    runtime: node
    plan: free
    buildCommand: cd web && npm install && npm run build && cd .. && npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
